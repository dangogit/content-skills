# Reel routing contract

- Route every request to `hebrew-captioned-video-reels`.
- Never use a second overlay-only pipeline.
- Verify source, target date, hook, caption source, and current schedule id/uuid.
- Render, audio, frame-zero, CTA, upload, and planner proof remain mandatory.
